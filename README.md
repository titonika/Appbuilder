
  # App Builder

  This is a code bundle for App Builder. The original project is available at https://www.figma.com/design/zPcoscAtm4STHjrSlAz3Pg/App-Builder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  